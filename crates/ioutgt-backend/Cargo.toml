[package]
name = "ioutgt-backend"
description = "Storage backends: null, memory, file (O_DIRECT), block device"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
ioutgt-core = { workspace = true }
ioutgt-uring = { workspace = true }
libc = { workspace = true }
# `spawn_local` for the sheepdog connection pump (queue threads run a
# current-thread runtime with a LocalSet; see `sheepdog::mux`).
tokio = { workspace = true, features = ["rt"] }
tracing = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
